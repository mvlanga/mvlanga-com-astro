pre-commit:
    parallel: false
    jobs:
        - name: format
          run: npx prettier {staged_files} --write
        - name: lint
          run: npm run lint:fix -- {staged_files}
        - name: check
          run: npm run check
