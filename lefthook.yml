commit-msg:
    commands:
        validate:
            run: npx --no -- commitlint --edit

pre-commit:
    jobs:
        - name: format
          run: npx prettier {staged_files} --write
        - name: lint
          run: npm run lint:fix -- {staged_files}
        - name: check
          run: npm run check
